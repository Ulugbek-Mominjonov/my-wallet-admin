{
  "name": "byudjet-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore xavfsizlik qoidalari testlari (emulyator)",
  "scripts": {
    "test": "vitest run",
    "emulator": "firebase emulators:exec --only firestore --project demo-byudjet \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.19.0",
    "firebase-tools": "^13.29.1",
    "vitest": "^3.0.0"
  }
}
